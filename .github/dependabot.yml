version: 2
updates:
  - package-ecosystem: "bundler"
    directory: "/"
    schedule:
      interval: "monthly"
    allow:
      # Update transitive dependencies too.
      - dependency-type: "all"
    groups:
      ruby-dependencies:
        update-types:
          - "minor"
          - "patch"
